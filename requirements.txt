# Core (mọi máy, mọi OS)
pyyaml
gradio
python-dotenv
requests

# Script generation
anthropic

# TTS
kokoro-onnx
soundfile
numpy

# Subtitles
faster-whisper

# Image gen fallback API (chua dung, de danh khi can)
# fal-client

# --- Image generation backend: chon THEM 1 trong 2 file duoi day theo may ---
# Mac Apple Silicon:      pip install -r requirements-mac.txt
# Windows/Linux NVIDIA:   pip install -r requirements-cuda.txt
